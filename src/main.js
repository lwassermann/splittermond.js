'use strict';

var React = require('react');

var ability = React.createClass({
  render() {
    return (
      <p class=""></p>
    );
  }
});

React.render(
  <h1>Hello, world!</h1>,
  document.getElementById('example')
);
